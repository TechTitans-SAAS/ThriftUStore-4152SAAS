<!-- app/views/layouts/_navbar.html.erb -->
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <%= link_to "ThriftyUStore", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <%= link_to items_path, class: "nav-link" do %>
            <i class="fas fa-store"></i> Marketplace
          <% end %>
        </li>
        <% if current_user %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-cog"></i> User
            </a>
            <div class="dropdown-menu" aria-labelledby="userDropdown">
              <a class="dropdown-item" href="<%= user_path(current_user) %>"><i class="fas fa-user"></i> My Profile</a>
              <a class="dropdown-item" href="<%= user_my_items_path(current_user) %>"><i class="fas fa-shopping-bag"></i> My Items</a>
              <a class="dropdown-item" href="<%= user_wish_list_path(current_user) %>"><i class="fas fa-heart"></i> Wishlist</a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="<%= destroy_user_session_path %>" data-method="delete"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "btn btn-outline-success" %>
          </li>
          <li class="nav-item ml-2">
            <%= link_to "Register", new_user_registration_path, class: "btn btn-success" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
